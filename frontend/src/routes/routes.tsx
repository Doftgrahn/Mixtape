import React, { FC } from 'react'
import { Route, Redirect } from 'react-router-dom'

const myRoutes = [{ name: 'home', path: '/' }]

const Routes: FC = () => {
  return <div>This is my Routes</div>
}

export default Routes
